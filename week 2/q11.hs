pyths :: Int -> [(Int, Int, Int)]
pyths n = [(x, y, z) | x <- [1..n], y <- [1..n], z <- [1..n], x^2 + y^2 == z^2]
{-
这个列表推导式定义了一个名为 pyths 的函数，它接受一个整数参数 n，并返回一个列表，其中包含所有满足勾股定理的三元组 (x, y, z)，其中 x、y 和 z 的值都不超过 n。

具体来说，这个列表推导式的语法形式为：
                                    [ expression | generator, ..., generator, predicate ]

其中 expression 是一个表达式，generator 是一个生成器，predicate 是一个谓词。列表推导式的作用是根据生成器和谓词生成一个列表，其中每个元素都是由 expression 计算得出的值。

在这个函数中，列表推导式的生成器是 x <- [1..n], y <- [1..n], z <- [1..n]，它生成了所有可能的三元组 (x, y, z)，其中 x、y 和 z 的值都不超过 n。谓词是 x^2 + y^2 == z^2，它检查三元组是否满足勾股定理。

如果满足，就将这个三元组添加到结果列表中。

因此，这个列表推导式的作用是返回所有满足勾股定理的三元组 (x, y, z)，其中 x、y 和 z 的值都不超过 n。
-}